export type CarouselItemType = {
    title: string,
    subTitle: string,
    carouselIamge: string,
    coursePathURL: string
};

export type Media = {
    slug: string,
    className: string,
    children: JSX
};