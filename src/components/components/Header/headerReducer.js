let initialState = {
    titleSchool: 'Школа',
    titleUniversity: 'Университет',
    titleIT: 'IT'
}

const headerReduser = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state;
    }
}